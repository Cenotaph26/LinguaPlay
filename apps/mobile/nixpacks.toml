[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["NODE_OPTIONS='--max-old-space-size=4096' npx expo export --platform web"]

[start]
cmd = "npx serve dist --single --listen $PORT"
